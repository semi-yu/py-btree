"""

"""
from node import Node


class BTree:
    def __init__(self,
                 order: int):
        self._order = order

        self._root = Node(
            order = order,
            is_root = True, is_leaf = True)
